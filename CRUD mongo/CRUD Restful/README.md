# This project contains the CRUD api using the MongoDB database with validations.

Clone this project and install all the dependencies.
    
    ```
    $ npm init
    
    $ npm body-parser express mongoose mongoose-unique-validator --save
    ```
